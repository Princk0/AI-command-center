# Set to 'true' to use simulated data (no backend needed)
# Set to 'false' and configure VITE_BACKEND_URL for real mode
VITE_MOCK_MODE=true

# Backend connection (used when VITE_MOCK_MODE=false)
VITE_BACKEND_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws
